# 🆕 NEW 标识功能说明

## 概述
为"故事精读模式"添加了动态的 NEW 标识，当有新文章进入系统时，原本的文件图标（📄）会被替换为红色的 "NEW" 文本，吸引学生的注意力。

---

## 🎯 功能位置

```
┌─────────────────────────────────────────┐
│  AI 语境训练 - 模式选择页面              │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────────────┐  ┌──────────────┐│
│  │ ┌────┐          │  │ 🧠           ││
│  │ │NEW │故事精读  │  │              ││
│  │ └────┘模式      │  │  逻辑演练     ││
│  │                  │  │  模式        ││
│  │  故事精读        │  │              ││
│  │                  │  │  完形填空     ││
│  └──────────────────┘  └──────────────┘│
└─────────────────────────────────────────┘
```

**显示位置：** 替换左侧卡片的图标位置

---

## 🎨 视觉设计

### NEW 标识样式

#### 无新文章时
```
┌────┐
│ 📄 │ ← 文件图标（FileText）
└────┘
```

#### 有新文章时
```
┌────┐
│NEW │ ← 红色文字，替换原图标
└────┘
```

**颜色：**
- 文字颜色：红色 `#ef4444`
- 悬停时：白色（卡片背景变黑）
- 无背景框，纯文字

**尺寸：**
- 字体：14px
- 字重：900（超粗）
- 字间距：0.15em（宽松）

---

## ✨ 动画效果

### 1. 脉冲动画（iconNewPulse）
```css
@keyframes iconNewPulse {
  0%, 100% { scale: 1.0, opacity: 1.0 }
  50%      { scale: 1.1, opacity: 0.9 }
}
```
- 周期：2秒（正常），1.5秒（悬停时）
- 效果：文字轻微放大缩小 + 透明度变化

### 2. 悬停状态变化
- **图标框变化**：
  - 背景：白色 → 黑色 `#0f172a`
  - 旋转 10° + 放大 1.1 倍
  - 阴影增强

- **NEW 文字变化**：
  - 颜色：红色 `#ef4444` → 白色
  - 放大：1.15 倍
  - 动画加速：2秒 → 1.5秒

### 3. 视觉层次
```
正常状态：
┌────────┐
│ ┌────┐ │
│ │NEW │ │ ← 红色文字，白色背景框
│ └────┘ │
└────────┘

悬停状态：
┌────────┐
│ ┌────┐ │ ← 旋转 10°，放大
│ │NEW │ │ ← 白色文字，黑色背景框
│ └────┘ │
└────────┘
```

---

## 🔧 技术实现

### 组件结构

#### ModeSelection.jsx
```jsx
const ModeSelection = ({ onSelectMode, hasNewStory = true }) => {
  const modes = [
    {
      id: 'mode_a',
      // ... 其他属性
      hasNew: hasNewStory // 控制是否显示 NEW
    },
    // ...
  ];

  return (
    <div className="mode-grid">
      {modes.map((mode) => (
        <button className="mode-card">
          {/* NEW 标识 */}
          {mode.hasNew && (
            <div className="new-badge">
              <span className="new-text">NEW</span>
            </div>
          )}
          {/* ... 其他内容 */}
        </button>
      ))}
    </div>
  );
};
```

#### AIContext.jsx
```jsx
<ModeSelection 
  onSelectMode={setStep}
  hasNewStory={true} // 控制是否显示 NEW
/>
```

---

## 📊 控制逻辑

### 1. 静态控制（当前实现）
```jsx
// 手动设置
hasNewStory={true}  // 显示 NEW
hasNewStory={false} // 隐藏 NEW
```

### 2. 动态控制（未来扩展）
```jsx
// 从状态管理获取
const [hasNewStory, setHasNewStory] = useState(false);

useEffect(() => {
  // 从后端 API 获取是否有新文章
  fetch('/api/stories/hasNew')
    .then(res => res.json())
    .then(data => setHasNewStory(data.hasNew));
}, []);

<ModeSelection hasNewStory={hasNewStory} />
```

### 3. 本地存储控制
```jsx
// 基于用户的最后阅读时间判断
useEffect(() => {
  const lastReadTime = localStorage.getItem('lastStoryReadTime');
  const latestStoryTime = getLatestStoryPublishTime();
  
  if (latestStoryTime > lastReadTime) {
    setHasNewStory(true);
  }
}, []);

// 用户点击进入后，标记为已读
const handleSelectMode = (mode) => {
  if (mode === 'mode_a') {
    localStorage.setItem('lastStoryReadTime', Date.now());
    setHasNewStory(false);
  }
  setStep(mode);
};
```

---

## 🎯 使用场景

### 场景 1：新文章发布
```
老师上传新文章 → 后端标记 hasNew = true
                     ↓
学生打开页面 → 看到 NEW 标识 → 吸引点击
                     ↓
点击进入阅读 → 标记为已读 → NEW 消失
```

### 场景 2：定期更新
```
每周一发布新故事
    ↓
周一上午 8:00 → NEW 出现
    ↓
学生阅读后 → NEW 消失
    ↓
下周一 8:00 → NEW 再次出现
```

### 场景 3：多篇文章管理
```jsx
const modes = [
  {
    id: 'mode_a',
    hasNew: hasNewStories,  // 根据未读文章数量决定
    badge: unreadCount > 0 ? `${unreadCount} 篇新文章` : null
  }
];
```

---

## 🎨 视觉效果演示

### 正常状态
```
┌──────────────────────┐
│ 📄                   │
│          NEW ⚡       │  ← 右上角
│                      │
│  故事精读模式        │
│                      │
│  在纯粹的 Web...     │
│                      │
│  开启激活 →          │
└──────────────────────┘
```

### 悬停状态
```
┌──────────────────────┐
│ 📄 (旋转+放大)       │
│          NEW ⚡       │  ← 放大 + 深色背景
│        (扩散光圈)     │
│  故事精读模式        │
│  (向右滑动)          │
│                      │
│  在纯粹的 Web...     │
│  (变深色)            │
│                      │
│  开启激活 → (紫色)   │
└──────────────────────┘
```

---

## 📝 CSS 关键样式

```css
/* NEW 标识容器 */
.new-badge {
  position: absolute;
  top: -8px;           /* 向上突出 8px */
  right: 1.5rem;       /* 距离右侧 1.5rem */
  z-index: 10;         /* 确保在最上层 */
  animation: float 3s ease-in-out infinite;
}

/* NEW 文本 */
.new-text {
  background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
  color: white;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.1em;
  padding: 0.35rem 0.75rem;
  border-radius: 1rem;
  
  /* 光晕效果 */
  box-shadow: 
    0 4px 12px rgba(239, 68, 68, 0.4),    /* 主阴影 */
    0 0 0 3px rgba(239, 68, 68, 0.1);     /* 外光圈 */
    
  animation: pulse 2s ease-in-out infinite;
}

/* 扩散光圈 */
.new-text::before {
  content: '';
  position: absolute;
  /* ... */
  animation: ping 2s cubic-bezier(0, 0, 0.2, 1) infinite;
}
```

---

## 🔍 浏览器兼容性

### 支持的特性
- ✅ CSS Gradients（渐变背景）
- ✅ CSS Animations（动画）
- ✅ CSS Transforms（变形）
- ✅ Box Shadow（阴影）
- ✅ Position Absolute（绝对定位）

### 兼容浏览器
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

---

## 🚀 未来扩展

### 1. 数字徽章
```jsx
{mode.hasNew && (
  <div className="new-badge">
    <span className="new-text">
      {unreadCount > 0 ? `${unreadCount}` : 'NEW'}
    </span>
  </div>
)}
```

### 2. 不同颜色主题
```jsx
// 根据内容类型显示不同颜色
const badgeColors = {
  new: 'red',      // 新文章：红色
  updated: 'blue', // 更新：蓝色
  hot: 'orange'    // 热门：橙色
};
```

### 3. 动态文本
```jsx
const badgeText = {
  new: 'NEW',
  updated: 'UPDATED',
  hot: 'HOT',
  recommended: '推荐'
};
```

### 4. 自动消失
```jsx
// 5秒后自动消失
useEffect(() => {
  if (hasNewStory) {
    const timer = setTimeout(() => {
      setHasNewStory(false);
    }, 5000);
    return () => clearTimeout(timer);
  }
}, [hasNewStory]);
```

### 5. 点击反馈
```jsx
const handleCardClick = (mode) => {
  if (mode.hasNew) {
    // 记录用户已查看
    markAsRead(mode.id);
    // 播放动画
    playConfetti();
  }
  onSelectMode(mode.id);
};
```

---

## 📋 测试清单

### 视觉测试
- [ ] NEW 标识位置正确（右上角）
- [ ] 颜色渐变显示正常
- [ ] 呼吸动画流畅
- [ ] 浮动动画自然
- [ ] 扩散光圈清晰
- [ ] 悬停效果明显

### 功能测试
- [ ] `hasNewStory={true}` 时显示
- [ ] `hasNewStory={false}` 时隐藏
- [ ] 点击卡片后正常进入
- [ ] 不影响原有交互

### 响应式测试
- [ ] 桌面端显示正常（>1200px）
- [ ] 平板端显示正常（768px-1200px）
- [ ] 移动端显示正常（<768px）

### 兼容性测试
- [ ] Chrome 浏览器
- [ ] Firefox 浏览器
- [ ] Safari 浏览器
- [ ] Edge 浏览器

---

## 🎓 设计理念

### 1. 注意力吸引（Attention Grabbing）
- **红色**：视觉上最抢眼的颜色
- **动画**：多重动画增强吸引力
- **光晕**：营造"发光"效果

### 2. 紧迫感（Urgency）
- **NEW 文本**：强调新内容
- **呼吸动画**：暗示"活跃"
- **悬停增强**：鼓励点击

### 3. 视觉层级（Visual Hierarchy）
- **位置**：右上角，视觉焦点
- **Z-index: 10**：确保在最上层
- **突出 8px**：超出卡片边界

### 4. 游戏化（Gamification）
- 类似游戏中的"新任务"提示
- 激发探索欲望
- 增强学习动力

---

## 总结

### 核心功能
- ✅ 动态 NEW 标识
- ✅ 三重动画效果（呼吸、浮动、扩散）
- ✅ 悬停增强反馈
- ✅ 可控显示/隐藏

### 设计价值
- **吸引注意**：红色标识 + 动画效果
- **提升点击率**：新内容提醒
- **增强用户体验**：即时视觉反馈
- **灵活可控**：通过 props 控制

### 技术特点
- 纯 CSS 动画，性能优秀
- 响应式设计，多端适配
- 易于扩展和定制
- 无第三方依赖

---

**更新时间**: 2026-01-30  
**版本**: v1.0 - NEW 标识功能上线
